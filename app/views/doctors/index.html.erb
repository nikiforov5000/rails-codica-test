<h1>Doctors#index</h1>
<p>Find me in app/views/doctors/index.html.erb</p>

<% @doctors.each do |doctor| %>
    <p><%= doctor.category.name %> | <%= doctor.first_name %> <%= doctor.last_name %>
        <%= link_to 'Make appointment', new_user_appointment_path(@user) %>
    </p>
<% end %>